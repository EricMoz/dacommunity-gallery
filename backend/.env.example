# OpenSea API key (required for fetch). Get one: https://docs.opensea.io/reference/api-keys
# For local dev only: python fetch_gallery_data.py --create-key  (writes key here; never commit .env)
OPENSEA_API_KEY=

# Optional: folder with dacat.*.png artwork for merge_local_images.py
# DACAT_USED_PICS_DIR=C:\path\to\Used Pics